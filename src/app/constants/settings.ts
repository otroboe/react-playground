import { IToasterProps, Position } from '@blueprintjs/core';

export const DEFAULT_TOASTER_PROPS: IToasterProps = {
  canEscapeKeyClear: true,
  className: 'toaster-container',
  position: Position.BOTTOM,
};
